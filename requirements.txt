# core
requests>=2.31
pydantic>=2.0
python-dotenv>=1.0
flask>=3.0
gunicorn>=21.2
psycopg[binary]>=3.2

# AI provider
openai>=1.100.0

# discovery
beautifulsoup4>=4.12
lxml>=5.1

# rendering (OpenAI gpt-image-2 returns base64 PNG; Pillow converts to JPEG)
Pillow>=10.0

# scheduling
apscheduler>=3.10

# publishing: image hosting for Instagram (S3-compatible — R2/S3/B2)
# Lazy-imported by src/publishing/image_host.py — only required at runtime
# when IG publishing actually fires.
boto3>=1.34

# tests
pytest>=8.0
responses>=0.25
